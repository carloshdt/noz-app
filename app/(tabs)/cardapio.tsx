import { View } from 'react-native';
import { AppText } from '../../components/ui/AppText';

export default function CardapioScreen() {
  return (
    <View className="flex-1 bg-background items-center justify-center">
      <AppText>Cardápio</AppText>
    </View>
  );
}
