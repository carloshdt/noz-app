# Corações em Receitas — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Usuários dão coração em receitas públicas no Feed e na tela de detalhe, com contagem e estado visual de coração próprio.

**Architecture:** Tabela `recipe_hearts` no Supabase. Hook `useCoracoes(receitaIds)` com Map de estado + toggle otimista. Feed passa IDs em batch ao montar; detalhe carrega individualmente. FeedCard recebe props `coracoes` e `onToggleCoracao`. `app/(tabs)/feed.tsx` instancia o hook e injeta dados.

**Tech Stack:** Supabase, React Native, NativeWind, lucide-react-native (Heart).

---

## Arquivos

| Ação | Arquivo |
|------|---------|
| Create | `hooks/useCoracoes.ts` |
| Create | `__tests__/hooks/useCoracoes.test.ts` |
| Modify | `components/FeedCard.tsx` |
| Modify | `app/(tabs)/feed.tsx` |
| Modify | `app/receita/[id]/index.tsx` |
| SQL | `supabase/migrations/20260504_recipe_hearts.sql` |

---

## Task 1: SQL Migration

**Files:**
- Create: `supabase/migrations/20260504_recipe_hearts.sql`

- [ ] **Step 1: Criar arquivo de migration**

```sql
create table recipe_hearts (
  id uuid primary key default gen_random_uuid(),
  recipe_id uuid not null references receitas(id) on delete cascade,
  user_id uuid not null references auth.users(id) on delete cascade,
  created_at timestamptz default now(),
  unique(recipe_id, user_id)
);

alter table recipe_hearts enable row level security;

create policy "Qualquer autenticado lê corações" on recipe_hearts
  for select using (auth.role() = 'authenticated');

create policy "Usuário gerencia próprio coração" on recipe_hearts
  for all using (auth.uid() = user_id);
```

- [ ] **Step 2: Aplicar migration no Supabase Dashboard (ou via CLI)**

```bash
# Via Supabase CLI (se configurado):
npx supabase db push
# Ou colar o SQL no Supabase Dashboard → SQL Editor
```

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/20260504_recipe_hearts.sql
git commit -m "feat: migration tabela recipe_hearts com RLS"
```

---

## Task 2: Hook `useCoracoes` (TDD)

**Files:**
- Create: `__tests__/hooks/useCoracoes.test.ts`
- Create: `hooks/useCoracoes.ts`

- [ ] **Step 1: Escrever testes**

```ts
// __tests__/hooks/useCoracoes.test.ts
import { renderHook, act } from '@testing-library/react-native';
import { useCoracoes } from '../../hooks/useCoracoes';

jest.mock('../../lib/supabase', () => ({
  supabase: {
    from: jest.fn(),
    auth: { getSession: jest.fn().mockResolvedValue({ data: { session: { user: { id: 'user-1' } } } }) },
  },
}));

jest.mock('../../hooks/useAuth', () => ({
  useAuth: () => ({ session: { user: { id: 'user-1' } } }),
}));

import { supabase } from '../../lib/supabase';

const mockFrom = supabase.from as jest.Mock;

function setupMock(data: any[], error: any = null) {
  const chain = {
    select: jest.fn().mockReturnThis(),
    in: jest.fn().mockResolvedValue({ data, error }),
    delete: jest.fn().mockReturnThis(),
    eq: jest.fn().mockReturnThis(),
    insert: jest.fn().mockResolvedValue({ data: null, error }),
    match: jest.fn().mockResolvedValue({ data: null, error }),
  };
  mockFrom.mockReturnValue(chain);
  return chain;
}

describe('useCoracoes', () => {
  beforeEach(() => jest.clearAllMocks());

  it('carrega corações em batch e monta Map', async () => {
    setupMock([
      { recipe_id: 'r1', user_id: 'user-1' },
      { recipe_id: 'r1', user_id: 'user-2' },
      { recipe_id: 'r2', user_id: 'user-3' },
    ]);

    const { result } = renderHook(() => useCoracoes(['r1', 'r2']));

    await act(async () => {
      await result.current.carregarCorações(['r1', 'r2']);
    });

    expect(result.current.coracoes.get('r1')).toEqual({ total: 2, meu: true });
    expect(result.current.coracoes.get('r2')).toEqual({ total: 1, meu: false });
  });

  it('retorna Map vazio quando sem corações', async () => {
    setupMock([]);
    const { result } = renderHook(() => useCoracoes(['r1']));

    await act(async () => {
      await result.current.carregarCorações(['r1']);
    });

    expect(result.current.coracoes.get('r1')).toBeUndefined();
  });

  it('toggleCoracao adiciona coração otimisticamente', async () => {
    setupMock([]);
    const { result } = renderHook(() => useCoracoes(['r1']));

    await act(async () => {
      await result.current.carregarCorações(['r1']);
    });

    setupMock([], null); // insert returns ok
    await act(async () => {
      await result.current.toggleCoracao('r1');
    });

    const estado = result.current.coracoes.get('r1');
    expect(estado?.meu).toBe(true);
    expect(estado?.total).toBe(1);
  });

  it('toggleCoracao remove coração otimisticamente', async () => {
    setupMock([{ recipe_id: 'r1', user_id: 'user-1' }]);
    const { result } = renderHook(() => useCoracoes(['r1']));

    await act(async () => {
      await result.current.carregarCorações(['r1']);
    });

    setupMock([], null);
    await act(async () => {
      await result.current.toggleCoracao('r1');
    });

    const estado = result.current.coracoes.get('r1');
    expect(estado?.meu).toBe(false);
    expect(estado?.total).toBe(0);
  });

  it('reverte estado otimista em caso de erro', async () => {
    setupMock([{ recipe_id: 'r1', user_id: 'user-1' }]);
    const { result } = renderHook(() => useCoracoes(['r1']));

    await act(async () => {
      await result.current.carregarCorações(['r1']);
    });

    // mock delete returns error
    const chain = {
      select: jest.fn().mockReturnThis(),
      in: jest.fn().mockResolvedValue({ data: [], error: null }),
      delete: jest.fn().mockReturnThis(),
      eq: jest.fn().mockReturnThis(),
      match: jest.fn().mockResolvedValue({ data: null, error: { message: 'DB error' } }),
    };
    mockFrom.mockReturnValue(chain);

    await act(async () => {
      await result.current.toggleCoracao('r1');
    });

    // revertido: meu voltou true, total voltou 1
    expect(result.current.coracoes.get('r1')).toEqual({ total: 1, meu: true });
  });
});
```

- [ ] **Step 2: Rodar testes para verificar falha**

```bash
npx jest __tests__/hooks/useCoracoes.test.ts --no-coverage
```

Expected: FAIL — `useCoracoes` not found.

- [ ] **Step 3: Implementar `hooks/useCoracoes.ts`**

```ts
import { useState, useCallback } from 'react';
import { supabase } from '../lib/supabase';
import { useAuth } from './useAuth';

type CoracaoEstado = { total: number; meu: boolean };

export function useCoracoes(receitaIds: string[]) {
  const [coracoes, setCoracoes] = useState<Map<string, CoracaoEstado>>(new Map());
  const { session } = useAuth();
  const userId = session?.user?.id;

  const carregarCorações = useCallback(async (ids: string[]) => {
    if (!ids.length) return;

    const { data, error } = await supabase
      .from('recipe_hearts')
      .select('recipe_id, user_id')
      .in('recipe_id', ids);

    if (error || !data) return;

    const mapa = new Map<string, CoracaoEstado>();
    for (const row of data) {
      const atual = mapa.get(row.recipe_id) ?? { total: 0, meu: false };
      mapa.set(row.recipe_id, {
        total: atual.total + 1,
        meu: atual.meu || row.user_id === userId,
      });
    }
    setCoracoes(mapa);
  }, [userId]);

  const toggleCoracao = useCallback(async (recipeId: string) => {
    if (!userId) return;

    const atual = coracoes.get(recipeId) ?? { total: 0, meu: false };
    const novoMeu = !atual.meu;
    const novoTotal = novoMeu ? atual.total + 1 : Math.max(0, atual.total - 1);

    // Update otimista
    setCoracoes(prev => new Map(prev).set(recipeId, { total: novoTotal, meu: novoMeu }));

    let error: any;
    if (novoMeu) {
      const res = await supabase
        .from('recipe_hearts')
        .insert({ recipe_id: recipeId, user_id: userId });
      error = res.error;
    } else {
      const res = await supabase
        .from('recipe_hearts')
        .delete()
        .match({ recipe_id: recipeId, user_id: userId });
      error = res.error;
    }

    if (error) {
      // Reverter
      setCoracoes(prev => new Map(prev).set(recipeId, atual));
    }
  }, [coracoes, userId]);

  return { coracoes, toggleCoracao, carregarCorações };
}
```

- [ ] **Step 4: Rodar testes**

```bash
npx jest __tests__/hooks/useCoracoes.test.ts --no-coverage
```

Expected: 5 passed.

- [ ] **Step 5: Commit**

```bash
git add hooks/useCoracoes.ts __tests__/hooks/useCoracoes.test.ts
git commit -m "feat: hook useCoracoes com toggle otimista e batch load"
```

---

## Task 3: Modificar FeedCard

**Files:**
- Modify: `components/FeedCard.tsx`

FeedCard recebe novos props: `coracaoTotal: number`, `coracaoMeu: boolean`, `onToggleCoracao: () => void`, `totalComentarios?: number`.

- [ ] **Step 1: Ler arquivo atual**

```bash
# Leia components/FeedCard.tsx antes de editar
```

- [ ] **Step 2: Adicionar props e barra de ações**

Adicionar ao tipo de props:
```ts
coracaoTotal?: number;
coracaoMeu?: boolean;
onToggleCoracao?: () => void;
totalComentarios?: number;
```

Adicionar imports:
```ts
import { Heart, MessageCircle } from 'lucide-react-native';
```

Adicionar barra de ações **abaixo da imagem e acima do conteúdo de texto**:

```tsx
{/* Barra de ações */}
<View className="flex-row items-center gap-4 px-4 py-2 border-b border-[#F5F0EB]">
  <Pressable
    onPress={onToggleCoracao}
    className="flex-row items-center gap-1"
    hitSlop={8}
  >
    <Heart
      size={22}
      color="#8B4513"
      fill={coracaoMeu ? '#8B4513' : 'none'}
    />
    <AppText className="text-[13px] font-sans-bold text-[#2C1810]">
      {coracaoTotal ?? 0}
    </AppText>
  </Pressable>
  <View className="flex-row items-center gap-1">
    <MessageCircle size={22} color="#8B4513" />
    <AppText className="text-[13px] font-sans-bold text-[#2C1810]">
      {totalComentarios ?? 0}
    </AppText>
  </View>
</View>
```

- [ ] **Step 3: Rodar testes gerais**

```bash
npx jest --no-coverage
```

Expected: sem regressões. FeedCard sem testes unitários próprios — sem novos failures.

- [ ] **Step 4: Commit**

```bash
git add components/FeedCard.tsx
git commit -m "feat: barra coração+comentário no FeedCard"
```

---

## Task 4: Integrar `useCoracoes` no Feed

**Files:**
- Modify: `app/(tabs)/feed.tsx`

- [ ] **Step 1: Adicionar hook e passar props ao FeedCard**

No `app/(tabs)/feed.tsx`, importar e instanciar:
```ts
import { useCoracoes } from '../../hooks/useCoracoes';

// Dentro do componente, após useFeed:
const receitaIds = receitas.map(r => r.id);
const { coracoes, carregarCorações, toggleCoracao } = useCoracoes(receitaIds);
```

Carregar corações quando feed carrega:
```ts
useEffect(() => {
  if (receitas.length > 0) {
    carregarCorações(receitas.map(r => r.id));
  }
}, [receitas]);
```

Passar props ao FeedCard:
```tsx
<FeedCard
  key={receita.id}
  receita={receita}
  // ... props existentes ...
  coracaoTotal={coracoes.get(receita.id)?.total ?? 0}
  coracaoMeu={coracoes.get(receita.id)?.meu ?? false}
  onToggleCoracao={() => toggleCoracao(receita.id)}
/>
```

- [ ] **Step 2: Rodar testes**

```bash
npx jest __tests__/hooks/useFeed.test.ts --no-coverage
```

Expected: sem regressões.

- [ ] **Step 3: Commit**

```bash
git add app/(tabs)/feed.tsx
git commit -m "feat: integrar useCoracoes no Feed"
```

---

## Task 5: Corações na tela de detalhe

**Files:**
- Modify: `app/receita/[id]/index.tsx`

- [ ] **Step 1: Adicionar hook e row de stats**

No `app/receita/[id]/index.tsx`:

```ts
import { useCoracoes } from '../../../hooks/useCoracoes';
import { Heart } from 'lucide-react-native';

// Após obter receita:
const { coracoes, carregarCorações, toggleCoracao } = useCoracoes(receita ? [receita.id] : []);

useEffect(() => {
  if (receita?.id) carregarCorações([receita.id]);
}, [receita?.id]);

const coracao = coracoes.get(receita?.id ?? '');
```

Adicionar row de stats **abaixo do header da receita** (abaixo do nome/imagem, acima dos ingredientes):

```tsx
{receita && (
  <View className="flex-row items-center gap-4 px-4 py-3 border-b border-[#F5F0EB]">
    <Pressable
      onPress={() => toggleCoracao(receita.id)}
      className="flex-row items-center gap-1"
      hitSlop={8}
    >
      <Heart
        size={20}
        color="#8B4513"
        fill={coracao?.meu ? '#8B4513' : 'none'}
      />
      <AppText className="text-[13px] text-[#2C1810]">
        {coracao?.total ?? 0}
      </AppText>
    </Pressable>
    <View className="flex-row items-center gap-1">
      <MessageCircle size={20} color="#8B4513" />
      <AppText className="text-[13px] text-[#2C1810]">0</AppText>
    </View>
  </View>
)}
```

(O `0` de comentários será substituído pelo hook useComentarios numa feature futura.)

- [ ] **Step 2: Rodar testes gerais**

```bash
npx jest --no-coverage
```

Expected: sem regressões.

- [ ] **Step 3: Commit**

```bash
git add app/receita/[id]/index.tsx
git commit -m "feat: corações e stats na tela de detalhe da receita"
```

---

## Verificação Final

```bash
npx jest --no-coverage
# Expected: todos passando, 0 failed
```

Manual (testar no device/simulador):
1. Feed → card tem ❤ 0 + 💬 0 na barra de ações
2. Tap no coração → preenche marrom + incrementa contador imediatamente
3. Tap novamente → volta outline + decrementa
4. Recarregar app → contagem persistiu (Supabase confirmou)
5. Tela de detalhe → mesma barra de stats funciona igual
